All cpp files can be compiled and ran through Visual Studio, except for problem 1, which was designed to be in a Linux Shell in the same directory as enrollments.txt

To run problem 1, compile it in Linux and link the file enrollments.txt as the input.